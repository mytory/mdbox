# AGENTS

여러 영상 도구를 담은 크로스 플랫폼 엘렉트론 앱. ffmpeg, ffprobe를 내장함.

## 커밋 규칙

커밋은 맘대로 하지 않습니다. 무조건 사용자의 승인을 받습니다. 사용자가 "커밋해"라고 해도 커밋 대상과 메시지를 사용자에게 보여준 뒤 진행합니다. 사용자의 승인을 받지 않은 메시지와 대상으로 커밋하지 않습니다.

푸시는 절대 스스로 하지 않습니다.

태그를 옮기는 것을 맘대로 하지 않습니다. 사용자가 옮기라고 명시적으로 지시할 때만 옮깁니다.

## 대화 규칙

대화도, 생각도 모두 한국어로 합니다.

### 코드 수정은 계획 승인 후에만

코드 수정, 파일 생성/수정/삭제는 무조건 다음 순서를 따릅니다:
1. 먼저 수정 계획을 제시합니다 (무엇을, 왜, 어떻게)
2. 사용자가 계획을 승인하면 그때서야 수정을 진행합니다
3. 사용자가 명시적으로 수정을 요청하기 전에는 절대 코드를 건드리지 않습니다

"답만 해", "말만 해", "설명만" 등 구두 설명만 요청하는 경우는 물론이고, 사용자가 별다른 지시를 하지 않았더라도 기본값은 "설명만"입니다. 사용자가 수정 의사를 밝히기 전까지 코드는 그대로 둡니다.


## 다국어화

향후 다국어화를 진행할 때 `.agents/skills/mdbox-translation` 스킬을 참고해서 진행합니다.


## 릴리즈 절차

1. 현재 `package.json` 버전을 확인합니다.
2. 변경사항을 확인하고, 다음 릴리즈 버전을 semver에 따라 결정합니다.
   - patch: 버그 수정
   - minor: 기능 추가
   - major: 호환성 깨는 변경
3. 결정한 이유를 사용자에게 보고하고 승인을 받습니다.
4. `package.json` 버전을 업데이트합니다.
5. 변경사항을 커밋합니다.
6. 태그를 만듭니다: `git tag -a vX.Y.Z -m "Release vX.Y.Z"`
7. 사용자에게 `git push origin master --tags` 또는 필요한 브랜치/태그 푸시 명령을 안내합니다.
